<?xml version="1.0" encoding="utf-8"?>
<paths>
    <!--
      Only the generated-forms folder is shared, never the database. A form is
      meant to leave the device; the passport records behind it are not.
    -->
    <cache-path name="forms" path="forms/" />
</paths>
